// import React from 'react'

import { gql, useQuery } from "@apollo/client"

/// type 'rfc' for 'react functional component' to auto-generate a new React Component

export default function ClimatePost() {
    return (
        <div>
            
        </div>
    )
}
